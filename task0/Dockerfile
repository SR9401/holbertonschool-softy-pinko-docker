# Utiliser l’image officielle Ubuntu la plus récente
FROM ubuntu:latest

# Mettre à jour les paquets et les mettre à niveau
RUN apt-get update && apt-get upgrade -y

# Définir la commande exécutée au lancement du conteneur
CMD echo "Hello, World!"
